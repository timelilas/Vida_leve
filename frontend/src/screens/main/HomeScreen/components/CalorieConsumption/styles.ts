import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  calorieProgress: {
    marginTop: 16,
    gap: 10
  },
  foodIconsWrapper: {
    gap: 32,
    rowGap: 16,
    flexWrap: "wrap",
    flexDirection: "row",
    justifyContent: "center",
    alignItems: "center"
  },
  linkButton: {
    marginTop: 8,
    alignSelf: "flex-end"
  }
});
