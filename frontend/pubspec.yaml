name: vida_leve
description: "Gordos no limite"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5                              
  cupertino_icons: ^1.0.6
  flutter_multi_formatter: ^2.2.0
  intl: ^0.18.0
  provider: ^6.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
