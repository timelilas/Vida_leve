import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    justifyContent: "space-around",
    alignItems: "center",
    flexWrap: "wrap",
    gap: 54
  },
  sectionContainer: {
    alignItems: "center"
  },
  sectionTitle: {
    marginBottom: 16
  },
  linkButton: {
    marginTop: 8
  }
});
