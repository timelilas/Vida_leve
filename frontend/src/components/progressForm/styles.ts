import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  form: {
    marginTop: 24,
    marginBottom: 40,
  },
  inputWrapper: {
    gap: 16,
  },
  planSelection: {
    marginTop: 32,
  },
  wrapper: {
    gap: 8,
    paddingBottom: 16,
  },
  submitButton: {
    marginTop: "auto",
  },
});
